[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ternary-market-direction-classifier"
version = "0.0.0"
description = "Ternary Market Direction Classifier"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["configs"]
